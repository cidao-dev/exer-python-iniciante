A = float(input())
B = float(input())
C = float(input())

peso_A = 2
peso_B = 3
peso_C = 5

Media = ((peso_A * A) + (peso_B * B) + (peso_C * C)) / (peso_A + peso_B + peso_C)
print(f'MEDIA = {Media:.2f}')
